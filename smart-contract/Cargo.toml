[package]
name = "stellar-contract"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = "20.0.0-rc.4"

[dev_dependencies]
soroban-sdk = { version = "20.0.0-rc.4", features = ["testutils"] }

[lib]
crate-type = ["cdylib", "rlib"]
